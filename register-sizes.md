# Register Sizes

## RBP & RSP Overview

In x86-64 assembly, `rbp` (base pointer) and `rsp` (stack pointer) are 64-bit registers.

## Default Size

Operations involving these registers typically move **8 bytes (64 bits)**.
